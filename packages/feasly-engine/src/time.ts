export function dtYears(periods: number): number[] {
  // Phase 1 stub: zeros; filled in later
  return Array.from({ length: periods }, () => 0);
}
export function discountFactors(periods: number): number[] {
  // Phase 1 stub: ones; filled in later
  return Array.from({ length: periods }, () => 1);
}